{
  "name": "challenge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install": "cd client && yarn && cd ../server && yarn",
    "dev-server": "cd server && yarn start:dev",
    "dev-client": "cd client && yarn start",
    "start-server": "cd server && yarn build && yarn start:prod",
    "start-client": "cd client && yarn build && live-server build/"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
